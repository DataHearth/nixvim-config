{
  plugins.treesitter = {
    enable = true;
    folding = true;

    settings = {
      auto_install = true;
      ensure_installed = "all";
      highlight.enable = true;
      indent.enable = true;
    };
  };
}
